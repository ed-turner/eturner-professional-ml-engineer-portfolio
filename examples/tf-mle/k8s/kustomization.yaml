apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
configMapGenerator:
    - name: example-configmap-1
      files:
        - metrics.config
        - batch.config
      namespace: "tensorflow"
resources:
    - deployment.yaml
    - service.yaml
